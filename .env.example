# T0101: environment template only; no runtime configuration is defined yet.
# Django (T0102), Next.js (T0103), and local services (T0104) will define keys.
# Add documented placeholder values with those tasks; never add real secrets.
# An optional local .env copy is ignored by Git.
