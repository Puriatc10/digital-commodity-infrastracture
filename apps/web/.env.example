# Copy to apps/web/.env.local before running the integrated application.
# Next.js does not load the repository-root Django/Compose .env.
# Server-only upstream for same-origin /api requests. Rebuild after changing it.
API_PROXY_TARGET=http://127.0.0.1:8000
