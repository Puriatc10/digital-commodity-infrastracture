# Copy to apps/web/.env.local before using the typed API client.
# Next.js does not load the repository-root Django/Compose .env.
# This public origin is embedded at build time; never put secrets here.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
